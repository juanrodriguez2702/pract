import { Component } from '@angular/core';

@Component({
  selector: 'app-prueba-nomenclatura',
  templateUrl: './prueba-nomenclatura.component.html',
  styleUrls: ['./prueba-nomenclatura.component.css']
})
export class PruebaNomenclaturaComponent {
  /*
  value: string="+"
  idlista: string='contenido_lista'
  idBtnDesple: string='desple'

  listap = [
    {id:'1', nombre:'AREPA', contenido:['elemento1', 'elemento2', 'elemento3']},
    {id:'2', nombre:'AREPA', contenido:['elemento1', 'elemento2', 'elemento3']},
    {id:'3', nombre:'AREPA', contenido:['elemento1', 'elemento2', 'elemento3']};

  ]

  desplegar(btn:string){
    const desple = document.getElementById('contenidolista' + btn)
    desple?.classList.toggle('mostrar')

    const button =  document.getElementById(this.idBtnDesple + btn)
    if(button){
      if (button.textContent === '+'){
        button.textContent ='-'
      }
      else if (button.textContent === '-'){
        button.textContent = '+'
      }

    }
  }
*/

}
